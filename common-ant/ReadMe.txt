this is a sample of common ant scripts integrating with ecplise. the script will parse the ecplise config file(.classpath) to find the dependent library.

the project is a SIP client demo. 

some SIP library is removed, just leave a sample of common ant.

feature:
1. parse .classpath with xlst.
2. define ant task.